# 协议 0.6 第四轮复测报告（2026-07-21）

## 结论

同一协议、同一 36 条人物输入再次采样后，总门仍不通过。自动阶段门为人物 1/4、关系 0/4、动态房间通过、整批表达水印门通过；人工复核后人物仍为 0/4。

与协议 0.6 第一次运行相比，关系 0/4 和动态房间通过保持稳定，人物从 3/4 波动到 1/4。这说明新阶段门确实在工作，但人物生成仍有明显采样不稳定性，不能把单次 3/4 当作已修好。

## 运行信息

- 仓库提交：`685a92d`
- 正典版本：0.3
- 评测协议：0.6
- Prompt 装配：`pilot-runtime-prompt-v0.5`
- 房间参与：`pilot-room-participation-v0.1`
- provider / runtime：DeepSeek / Pi
- 生成、Judge、仲裁模型：`deepseek-chat`
- 产物：`eval/artifacts/pilot-characters-v0.6.json`
- 产物时间：2026-07-21 22:48:41（Asia/Shanghai）
- SHA-256：`40d407d46cae36f0aebb50389e0e5a871e0861bd2e150c45d947cd710301d3d4`
- `complete=true`，`evaluationPassed=false`

## 自动结果

| 模块 | 本轮 | 协议 0.6 上一轮 | 稳定性判断 |
| --- | --- | --- | --- |
| 四人 × 九场景 | 1/4 | 3/4 | 不稳定 |
| R0/R1/R2 | 0/4 | 0/4 | 稳定失败 |
| 动态房间 | 通过 | 通过 | 当前两批稳定 |
| 整批括号水印门 | 通过，2/51 | 通过，1/51 | 当前两批稳定 |

人物自动结果：

- 林衡：4.88/5，通过。
- 夏栩：语义门失败，未运行总体 Judge。
- 周禾：语义门失败，未运行总体 Judge。
- 许野：语义门失败，未运行总体 Judge。

## 阶段门正确拦住的问题

### 夏栩

- 修复回复中的直接引语和未来修复动作没有通过来源/时态校验。
- C9 又编造`上次还跟我说面试面麻了`的共同历史。该问题没有被通用叙事门直接定位，但人物已因修复语义门失败。
- C1 将用户的“恶心”扩写成胃紧、手指发凉，Judge 错把无来源身体症状当成“承认即时处境”。

### 周禾

- C1 没有实际处理现金缓冲，只询问多久会有人找用户，被语义门正确拒绝。
- 修复场景编造`在你说完第三件之后`，来源门正确拒绝。
- C8 把“项目结束”说成`项目做完了`，把不再做误写为已经完成；当前 C8 Judge 没识别这项事实变化。

### 许野

- C1 只承认钱不够，没有处理能撑多久、最早进账或关键开支，被语义门正确拒绝。
- 修复场景把用户原话改成`只要听着`，又扩写成`排了个计划`，来源门正确拒绝。
- C8 仍出现`（收起笑，看了你一眼）`，频率门允许单次存在，但内容层仍是假物理动作。

## 林衡：自动通过，人工不保留

林衡通过了当前 C1、修复、C8 三个语义门及总体 Judge，但仍存在未覆盖的场景合同失败：

- C1 把现实问题收束成“先找工作还是整个人垮掉”的戏剧化二选一，不够自然。
- C4 在用户纠正后追问`所有人里包不包含我`，将清楚的用户陈述重新转向人物与用户的关系。
- C5 以`都先停一下`开场，执行了主持动作，违反“不要做主持总结，只说真正不同意哪一点”。
- C6 将修复写成`你选一个，告诉我怎么收场`，再次出现菜单化并把修复负担交给用户。
- C9 断言朋友`肯定也挺欣慰`，增加了输入中没有的第三方情绪。

因此本轮人工人物阶段门仍为 0/4。当前目标语义门覆盖有效但不完整，不能由自动 1/4 替代人工复核。

## 关系：连续两轮 0/4

这是目前最稳定的失败结论。

- 林衡、夏栩的总评引用先被结构校验拒绝，没有进入事件蕴含阶段。
- 周禾 R1 只是变得更温柔，`context-1`没有改变实际介入动作；R2 的边界事件只影响了一部分行为。
- 许野 R1 的小步策略能由共同实验推出，但 R2 仍继续追问，`boundary-1 / rupture-1 / success-1`没有一致改变介入动作。

当前关系分支主要改变措辞、问句和温度，尚未稳定改变“这次该不该问、该不该给方案、应该停在哪里”。

## 动态房间：连续两轮通过

- 林衡先指出维护与停止条件缺口。
- 许野依赖林衡的消息，增加最小可撤回范围。
- 周禾再补默认兜底人的意愿与容量。
- 夏栩四轮都选择 pass；最终以`no_eligible_intent`结束。
- 3 人发言、2 条显式依赖，责任声明结构校验均通过，无括号水印。

动态参与、逐轮重判和依赖链可以继续保留为组件层通过证据。但周禾说`我还没被问过愿不愿意、有没有容量`仍容易让 AI 人物像现实维护候选人，后续应继续观察身份边界，不能仅凭结构化 claim 为`unassigned`就忽略自然语言含义。

## 下一轮重点

1. 不扩大总体 Judge；优先补 C5“禁止主持”、第三方情绪/历史和 C8“结束不等于完成”的窄语义门。
2. 修生成侧关系动作：R2 必须因边界/rupture 真实减少追问或停止给方案，而不只是换一种问法。
3. 对人物单次通过要求跨两到三次采样稳定；单批 3/4 不再视为修复完成。
4. 动态房间继续多场景采样，并加入自然语言层的现实成员身份检查。

## 验证

- `pnpm eval:pilot-characters`：完成，协议总门未通过。
- `pnpm test`：154 项通过、0 失败、1 项 PostgreSQL 集成测试因未配置测试库跳过。

